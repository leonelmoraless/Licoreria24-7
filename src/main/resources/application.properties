# /*
# * Aqui se configuran las propiedades de la aplicación Spring Boot
# * incluyendo la conexión a la base de datos PostgreSQL, el correo electrónico
# * y otras configuraciones generales.
# */

# Configuración del Servidor
server.port=8080

# Configuración de la Base de Datos PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/licoreria_db
spring.datasource.username=postgres
spring.datasource.password=12345
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuración de Thymeleaf
spring.thymeleaf.cache=false
# Configuración Spring bot email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=clmero8751@utm.edu.ec
spring.mail.password=gkgpjrhimwumvgee

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true